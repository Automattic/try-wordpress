{
    "definitions": {
        "field": {
            "type": "object",
            "properties": {
                "type": {
                    "description": "The type of field",
                    "type": "string",
                    "enum": [
                        "date",
                        "text",
                        "html"
                    ]
                }
            },
            "required": [
                "type"
            ],
            "additionalProperties": false
        }
    },
    "type": "object",
    "properties": {
        "title": {
            "description": "The subject's title.",
            "type": "string"
        },
        "slug": {
            "description": "The subject's unique slug. Must be a sequence of lowercase letters, potentially with underscores.",
            "type": "string",
            "pattern": "^[a-z]+(?:_+[a-z]+)*$"
        },
        "fields": {
            "description": "List of fields.",
            "type": "object",
            "additionalProperties": {
                "$ref": "#/definitions/field"
            }
        }
    },
    "required": [
        "title",
        "slug",
        "fields"
    ],
    "additionalProperties": false
}
